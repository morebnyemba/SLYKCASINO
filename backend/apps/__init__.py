"""Domain apps package. Each subpackage is a self-contained bounded context
following the identical symmetric structure (models/, services, clients, utils,
helpers, recovery, tasks, dtos, + DRF transport). See apps/README.md.
"""
